name: remind
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch
      - torchvision
      - transformers
      - scikit-learn
      - scipy
      - numpy
      - opencv-python
      - tqdm
      - psutil
      - ultralytics
      - rfdetr
